# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# Your personal Telegram chat ID (get from @userinfobot)
TELEGRAM_CHAT_ID=

# Optional: tune scoring threshold (0-100). Lower = more alerts. Default 60.
COMPOSITE_THRESHOLD=60

# Optional: minutes before re-alerting same stock. Default 60.
COOLDOWN_MINUTES=60

# Optional: scan interval in seconds. Default 300 (5 min).
# When using Fyers WebSocket (real-time ticks), drop this to 60.
SCAN_INTERVAL_SECONDS=300

# ============================================================================
# Fyers broker credentials (real-time price feed)
# ============================================================================
# Create an API app at https://myapi.fyers.in → "Create App" (App Type: 100).
# Enable 2FA-via-TOTP from Fyers MyAccount; copy the base32 secret BEFORE you
# scan it into your authenticator app — Fyers does not show it again.
# WARNING: anyone with read access to this file can place trades on your
# account. Keep .env out of git, off shared drives, and consider an OS keyring
# if this ever leaves your laptop.
# ============================================================================

FYERS_APP_ID=
FYERS_SECRET_ID=
FYERS_REDIRECT_URI=
FYERS_USER_ID=
FYERS_PIN=
FYERS_TOTP_SECRET=
